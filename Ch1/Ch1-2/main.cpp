#include <iostream>

int main() {
    std::cout << "Hello, World!\n"; //std::cout lives in the iostream library
    std::cout << " \n"; // These comments make the code hard to read
    std::cout << " \n"; // especially when lines are different when lines are different lengths

    // It's a better way.
    std::cout << " \n";

    /*
     * This is a multi-line comment.
     * This layout makes the comment easier to read.
     */

    /*
     * GOOD COMMENTS AND BAD COMMENTS
     */
    std::cout << " \n";
    return 0;
}
